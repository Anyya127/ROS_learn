import rclpy
from rclpy.node import Node

def main():
    rclpy.init()#初始化工作分配资源
    node=Node('python_node')#创建一个节点
    node.get_logger().info('Hello ROS2 Python Node!')#输出日志信息
    node.get_logger().warn('Hello ROS2 Python Node!')#输出日志信息
    rclpy.spin(node)#保持节点运行
    rclpy.shutdown()#关闭节点
